---
import { labEntries } from '../../data/lab';

export function getStaticPaths() {
  return labEntries.map(entry => ({
    params: { slug: entry.slug },
    props: { destination: entry.url },
  }));
}

return Astro.redirect(Astro.props.destination, 301);
---
